# JobMatix Revival Roadmap v3

**Last Updated**: September 2, 2026
**Supersedes**: `ROADMAP-ARCHIVE-2026-08.md` (v2 — Phase 2 was accurate and is now complete; Phase 3 was a thin, partly-wrong placeholder, corrected below after a real code audit)
**Project**: Martin Fenwick, solo maintainer, AI-assisted (Claude Code)
**Status of the software**: JobMatix/JMxPOS was last live in production ~2020–2021 at Precise PCs. It is **not currently running anywhere**. This is a revival for a fresh multi-store IBG rollout, not a live cutover.

## 2026-09-02 session write-up and pick-up-here-next

Picked up directly from yesterday's two flagged open items, then continued porting unblocked Phase 3 features. Everything below is committed and pushed to `feature/postgresql-migration`, verified live against real migrated data.

**Yesterday's two open items, resolved:**
- **Transaction Lookup customer-barcode "mixed customers" bug** - root-caused as a real race, not a query bug (the SQL was always correct, exactly as suspected yesterday). `TransactionLookupViewModel.Search()` was reachable both via `SearchCommand` (which CommunityToolkit's `AsyncRelayCommand` guards against re-entrancy) and via direct calls from `SelectInvoiceByIdAsync`/`ConfirmVoid` (F8 "Show Last Invoice", the Customer tab's invoice links, post-void refresh) that bypass that guard entirely. Two overlapping searches - e.g. a customer-filtered search still awaiting the DB, clobbered mid-flight by an unrelated unfiltered lookup - interleaved their results into the same `Transactions` collection. Fixed with a generation token: only the most recently started `Search()` call is allowed to touch `Transactions`. Same pattern is worth checking anywhere else a ViewModel exposes both a guarded command and an unguarded direct-call path to the same load method (see the tab-switch bug below, a different instance of the same underlying class of issue).
- **Transaction Lookup's dead "Quote" search type** - retired rather than wired up. It searched `invoice.transactiontype='QUOTE'`, a value nothing in the app has ever written (the real Quote → Job → Invoice pipeline is job-centric, `quote_job_parts`/`jobs.customerinstruction`, not invoice-based) - always returned zero rows. `CustomerService.GetCustomerQuotesAsync` (feeding the Customer screen's "Quotes" sub-tab) had the identical bug, found while fixing the first - retired both, plus the now-pointless sub-tab, rather than inventing a new invoice-side QUOTE event for a pipeline that has no such moment today. Build quotes are already surfaced on the ticket itself via the Quoted Components section.

**Brand/model reference data wired into job intake** - closes the fast-follow flagged in Phase 3 below. `GoodsTypes`/`Brands`/`Symptoms` (built 2026-09-01, never actually consumed anywhere) are now real ComboBoxes at ticket intake, replacing free text, loaded via `ReferenceDataService.GetAllAsync` alongside the existing open-jobs load. `TaskTypes` still isn't consumed anywhere (no task-recording feature exists yet). No schema change - the job columns stay plain varchar, not FKs to the reference tables.

**Two real, severe, pre-existing bugs found while building today's features - neither introduced today:**
- **Creating any new job/ticket has been silently broken since the columns were last touched.** `JobService.CreateJobAsync`'s `RETURNING` clause hand-duplicated a column list that was missing `customercompany`/`username`/`userpassword`/`goodsother` - four columns `ReadJob(reader)` always expects. Every `CreateJobAsync` call threw `IndexOutOfRangeException`, and since `JobViewModel.CreateJob()`'s `[RelayCommand]` has no try/catch around that line, the exception became an unobserved task exception - clicking "Create Ticket" did nothing visible at all, no error, no new ticket. Fixed by having `RETURNING` reuse the shared `SelectColumns` constant instead of its own hand-maintained copy (every other job query already did this correctly - `CreateJobAsync` was the one place that didn't). Found by chance while adding `datepromised` to the same column list for the on-site feature below; worth remembering that any query hand-duplicating a shared column list is a standing risk in this codebase.
- **Every list-loading tab (Stock, Customers, Stocktake, Goods Received, Return Authorisations, Jobs, Reference Data) shows every row twice when navigated to via a menu item/keyboard shortcut** (not via plain tab-strip clicking). Root cause: `MainWindowViewModel.OnSelectedTabIndexChanged` auto-fires `LoadTabDataAsync(tabIndex)` on any change to `SelectedTabIndex`, fire-and-forget - and the `Jobs()`/`StockList()`/`FindStock()`/`CustomerList()`/`FindCustomer()`/`Stocktake()`/`GoodsReceived()`/`ReturnAuthorizations()`/`OpenReferenceData()` commands ALSO set `SelectedTabIndex` and then explicitly await the exact same load, so both fire concurrently and double-populate the target `ObservableCollection`. Found because it made the on-site jobs list visibly show every ticket twice while testing. Fixed with a `_suppressTabAutoLoad` flag wrapped around each command's `SelectedTabIndex` assignment, so the command's own explicit (and still necessary - it's what makes re-clicking the menu item while already on that tab actually refresh) load is the only one that runs.

**On-site job list + staff SMS reminder poller - shipped** (closes the Phase 3 item below). `DatePromised` was wired onto `JobRecord`/`JobService` for the first time (never read anywhere before today) - also fixed its Phase 1 hardcoded-default bug (`2020-12-25 NOT NULL`) to a plain nullable column with no default, since a real "not yet promised" job should store NULL, not a sentinel; the ~22,783 existing rows already sitting at the old 2020-12-25/2050-12-25 sentinels were deliberately left untouched (real migrated history), with `JobRecord.DatePromisedSentinels`/`HasRealDatePromised` guarding against treating those as real dates in any query. Intake gained a Nominated Tech barcode field and a promised-date picker (both optional). "ON-SITE JOB;" - the exact literal the legacy app used to flag an on-site job by overwriting the entire `goodsincare` field instead of a normal goods type - was seeded into the `goodstypes` table (`sql-scripts/seed-onsite-goodstype.sql`) so it's selectable from the same dropdown just wired up above; 2,080 real migrated jobs already carry this value. Built: a read-only "On-Site Jobs" report (Reports tab, reuses the existing grid/sort/export machinery) listing any open on-site job by promised date; and a background reminder poller (`JobViewModel`, a `System.Threading.Timer` on a 15-minute interval, opening its own short-lived DB connection per poll rather than holding one open) that SMS's the nominated/assigned tech once per job per day when an on-site job flagged `05-WaitListed`/`10-Created` is promised for today, deduped via the previously-unused `jobother` table. Deliberately simplified from the legacy version's 2-stage "wake up + reminder" cadence (one reminder is enough) and defaults **off** (`systeminfo.EnableOnSiteSmsReminders`, config-via-DB like the labour-rate keys, no dedicated settings UI) - matching the legacy default, so a fresh store doesn't suddenly start texting staff. A real data quirk found while verifying: `docket_name` isn't unique in real staff data (two rows both named "Jay") - the nominated-tech mobile lookup uses a `LIMIT 1` scalar subquery, not a `JOIN`, specifically to avoid fanning one job out into duplicate reminder candidates (and duplicate sends) because of this.

**Staff password hashing - shipped** (closes a Phase 1 open item). Investigated before touching anything: `staff.password` turned out to be genuinely vestigial - this app's real identity mechanism is barcode/staff-number entry (a deliberate communal-till design, not an oversight - see `MainWindowViewModel`'s own comment on `IsStaffAdminUnlocked`), and all 45 real staff rows had `password = ''`; nothing anywhere compares against it. Hashed anyway (PBKDF2 via `Rfc2898DeriveBytes`, no new NuGet dependency - matches this project's existing preference for stdlib over adding a package, see the PDFsharp-over-QuestPDF licensing note), since the column may become load-bearing later (a PIN/password admin-override upgrade is noted as deferred, not dropped) and there's no reason to wait for that day to fix it. `staff.password` widened from `varchar(80)` to `text` (a hash + salt + iteration count doesn't comfortably fit in 80 chars). Zero migration needed - no real plaintext data existed to convert. Also fixed a real, if minor, bug the same investigation turned up: the Staff edit form used to round-trip the stored value straight back into an editable plaintext-looking field (`StaffViewModel.LoadStaffToForm`) - harmless only because the field was always empty; now the field is always blank on load ("leave blank to keep unchanged"), and only a non-blank entry gets hashed and saved, the existing hash preserved otherwise. Verified live: saving with the field blank preserves the existing (empty) value; setting a password stores a real hash that verifies correctly.

**`jobs.username`/`jobs.userpassword` encryption - shipped** (closes the last Phase 1 plaintext-credential item, after `staff.password` earlier this session). Confirmed first (direct instruction) that this data is genuinely reversible-required, not hashable: it's printed verbatim on the job docket (`JobDocumentPdfService.DrawUserLogonSection`) so a tech has it on paper while working, matching the legacy app's own behaviour - any fix has to decrypt back to real plaintext at that point. Built `Services/CredentialEncryptor.cs` (AES-256-GCM via the BCL `AesGcm` class, no new NuGet dependency), key in `.env` as `JOBMATIX_JOBS_CREDENTIAL_KEY` (gitignored, with an explicit warning that it must be backed up separately from the database - losing it permanently orphans this data, a DB backup alone can't recover it). `jobs.username`/`userpassword` widened from `varchar(32)` to `text` first (ciphertext is longer than plaintext). Decryption happens inside `JobService.ReadJob` only - every other caller keeps working with real plaintext, no ripple elsewhere in the app. There's no live write path for these fields today (confirmed: `CreateJobAsync`'s INSERT never included them, and no UI form edits them - they're only ever populated by the original historical migration), so no encrypt-on-write path was needed either; if a UI to edit these is ever added, it must route through `CredentialEncryptor.Encrypt`.

**Real data migration, done carefully given the stakes** (real customer PC credentials, ~7,600 rows, and a lost key means permanent unrecoverable loss): took a real `pg_dump -Fc` backup first (`db-backups/jobmatix_pos_pre-credential-encryption_20260902-105627.dump`, restore-tested with `pg_restore -l` before proceeding, matching the project's existing pre-merge backup convention), then ran a one-time migration inside a single DB transaction that encrypted all 7,593 affected rows and **verified every single row decrypts back to its exact original plaintext before committing** (would have rolled back with zero changes on any mismatch - there were none). Confirmed after commit: 6,681/7,591 rows now carry the `ENC1:` ciphertext prefix (matching the original username/password population counts exactly), zero plaintext left in either column. Re-verified independently through the real app read path (`JobService.GetJobByIdAsync`) and by actually re-rendering a real job's docket PDF and confirming the printed username/password match the pre-migration values.

**Pick up here next**: no fresh live-testing loop running this session (unlike 2026-09-01) - the remaining "known open items" below are the smallest next steps. The two newly-found bugs above (CreateJobAsync, tab-switch double-load) are worth specifically re-testing once real usage resumes, since both were silent (no error shown to the user) and could easily still be masking similar sibling bugs not yet found.

**A third, minor UX inconsistency noticed while testing the password fix, not fixed**: `StaffViewModel.LoadStaffAsync` (and several sibling `Load*Async` methods elsewhere) reassign their `ObservableCollection` wholesale (`StaffMembers = new ObservableCollection<Staff>(items)`) rather than `Clear()`+`Add()`-ing into the existing instance - this silently drops the UI's current list selection on every reload (confirmed: saving a staff edit, which reloads the list afterward, deselects whatever was selected). Not a correctness bug, just a rough edge - worth a look if "why did my selection disappear" ever comes up as real feedback.

**Job docket printing: Receipt shipped, the other 3 remaining types correctly scoped as blocked/non-trivial rather than assumed easy** - see Phase 2's own entry above for the full detail. Investigated before building anything further: Job Labels needs a real barcode-font capability this app doesn't have, Service/Delivery Record and Customer Maintenance Report both need genuine new multi-page pagination logic PDFsharp doesn't provide for free, and Customer Maintenance Report's photo column is flatly blocked on attachment storage not existing yet (same category as Quote printing's block). Receipt was the one that was actually tractable - reads back the real persisted invoice rather than recomputing figures independently.

---

## 2026-09-01 session write-up and pick-up-here-tomorrow

This was one very long, very active session of the owner live-testing the running app and reporting bugs/gaps in rapid rounds, all in direct response to real usage - not a planning session. Everything below shipped today, is committed and pushed to `feature/postgresql-migration`, and was verified live against real migrated data before being called done. Full blow-by-blow detail (exact root causes, code quoted, verification steps) is in this file's "UI/UX notes" and numbered "follow-up round" entries under Phase 3, and in the "Quote → Job → Invoice pipeline" entries near the end of Phase 6 - this section is the condensed version for picking the thread back up quickly.

**Navigation, ticket UI, and time tracking** - tab strip cut down to Sale/Stock/Customers/Tickets (everything else moved to the menu bar), Jobs renamed Tickets throughout, ticket search + a restored status-grouped tree list with counts, a notes section (color-coded public/private) on the ticket detail panel, click-through from a customer's ticket/invoice history straight to the ticket/invoice itself, and a full concurrent ticket-timer feature (any number of tickets can each have their own running timer, a status-bar "N timers running" indicator that jumps to a filtered list, stopping a timer auto-logs a private note - billing integration deliberately not wired up).

**Reports page rework** - date-range/customer-barcode controls now only show for reports that actually use them, only the genuinely active report is highlighted (Cash Up used to be hardcoded green permanently), descriptive per-report column headers with numeric/date-aware click-to-sort, and a Refresh button since changing the date range didn't used to re-run anything. Two real report-data bugs fixed in the process: Stock Value was undercounting by ~95% (a row cap applied before the report's own filter), and Low Stock always showed 0 (the query never selected the reorder-level columns at all, not a data gap).

**Color palette + Customer Instruction approval flow** - all yellow/orange chrome (timer box, ticket-info boxes, private notes) replaced with a two-tone blue system per direct feedback. Restored a legacy approval field this port had dropped (Quotation Required / Proceed with Service / Proceed to Limit, required at ticket intake, color-coded banner on the ticket page) plus an Approve action for upgrading a Quotation Required ticket once a customer calls back to confirm - this surfaced an important business-domain distinction, confirmed with the owner: there are two different kinds of "quote" in this business (a repair-diagnosis quote, which is what this models, vs. a build-component quote, which is the pipeline below) - keep them conceptually separate in any future work here.

**Four real, severe bugs found and fixed**, none introduced this session - all pre-existing, just newly surfaced by live testing: (1) **Commit Sale silently doing nothing all session** - the Sale screen's staff-number field only resolved on the Enter key (matching a barcode scanner), with no fallback for someone typing it by hand and tabbing away; fixed with a `LostFocus` handler. (2) Transaction Lookup showing the **VOIDED badge on almost every transaction** regardless of real status (wrong `IsVisible` binding logic - real data: 1 genuinely voided invoice out of 39,884). (3) Transaction Lookup's **Type/Period dropdowns never actually worked** (`SelectedItem` bound to a plain string against inline `ComboBoxItem` children, which never match) - same fix applied to 3 other pickers found via grep (Return Authorisation's Origin/Return Result, ticket-intake Priority). (4) The Sale screen's Remove button was a truncated text label, now a minus icon.

**The full Quote → Job → Invoice pipeline, built and shipped in 4 phases** - this was the day's biggest single piece of work. Triggered by the owner asking why quotes "weren't populating" for a customer account used specifically for that; the real answer was that historical quote data (7,842 rows, a whole legacy table, `quote_job_parts`) had been sitting migrated and completely unused since the database merge - I'd wrongly told the owner no such data existed, having only checked the Sale/Invoice domain instead of the Job domain where it actually lived. Built: (1) serial number capture on real Parts, blocking completion until satisfied; (2) a "Quoted Components" section on any Quotation-Required ticket, live for new tickets and surfacing the historical data too; (3) Approve auto-converts quoted components into real parts, locking in the quoted price; (4) completing a job now generates a real invoice (`JobService.CompleteJobAndInvoiceAsync`), reusing `SaleService.CommitSaleAsync`'s exact proven transaction shape - correct tax/cost/margin math, real stock decrement, a labour line from tracked billable time. All four phases verified live end-to-end against real test data, including a forced idempotency-check failure that correctly rolled back cleanly (job stayed at its prior status, not stuck "completed but uninvoiced").

**Known open items, not yet fixed:**
- **The already-documented fire-and-forget `HandleSelectionChangeAsync` race** (in `JobViewModel.OnSelectedJobChanged`) is now a slightly sharper edge than before, since the new serial-completion validation depends on `Parts` being loaded - a human clicking through has enough natural delay that this is unlikely to bite, but it's the same underlying issue flagged earlier in this file, not newly introduced.
- Phase 6.2 (`franky-forge` central catalog sync) and the PriceIQ scan-back/promotion integration noted under it are both still unstarted on the JobMatix side - do not push or otherwise touch `franky-forge` without an explicit go-ahead.

*(The other items originally listed here - the Transaction Lookup customer-barcode bug, the dead "Quote" search type, the Brand/model reference-data wiring, both plaintext-credential gaps, and the Receipt document type - were all resolved 2026-09-02, see that session's write-up above. Job docket printing's remaining 3 document types are correctly re-scoped as blocked/non-trivial, also see above, not simply "not yet picked up.")*

**Pick up here tomorrow**: this has been a fast, live-testing-driven feedback loop all day (the owner running the app and reporting exact issues as they hit them) rather than a planned backlog - the most likely next step is simply continuing that same loop. If starting cold instead, the two "known open items" above (customer-barcode filter re-check, Quote search-type decision) are the smallest, most self-contained loose ends directly related to today's work.

---

## What Changed From v2 (and why)

v2 got Phase 2 right and shipped it. Phase 3 was still a guess — a thin bullet list written before anyone had actually read `JMxJT620.NET`. This version replaces that guess with findings from a real line-by-line audit of the legacy Job Tracking, Return Authorisations, and launcher source, cross-checked against the live `jobmatix_jobs` Postgres database (which already holds real migrated data: 26,383 jobs, 30,888 parts, 46,571 tasks, 1,323 return authorisations).

1. **Phase 2 is done.** Cash-up, Stocktake, Customer Statements, Goods Received, and Transaction Lookup/Void all shipped and were verified against live data this session. What's left in Phase 2 is only the items that were always blocked on a decision (receipt printing/cash drawer hardware, subscriptions, email/SMTP) — see Phase 2 below.
2. **"Zero foreign keys in the Jobs database" (v1/v2's Phase 1 gap) is stale — already fixed.** `create-jobs-schema-extensions.sql` added 10 FK constraints (jobs→documents/jobchecklists/jobother/parts/returnauthorizations/tasks/job_service_checklists, plus quote_job_parts, ra_attachments, tasktypes) before this session started. Verified live. Don't keep re-flagging this.
3. **A new, previously-unknown security gap found**: `jobs.username`/`jobs.userpassword` store **customers' own PC login credentials** in plaintext (collected at intake so a tech can log into the machine being repaired) — 6,681 of 26,383 real jobs have a populated username, 7,591 have a password. This is distinct from the already-known `staff.password` plaintext issue (that one's an internal login; this one is customers' personal credentials, arguably worse exposure). Both need fixing, treat as two separate line items.
4. **A second instance of Phase 2's missing-SERIAL-sequence bug found and fixed in the Jobs database.** Same root cause as the 17-table bug already fixed in `jobmatix_pos`: `quote_job_parts`, `model_checklist`, `ra_attachments`, `job_service_checklists` were declared `INTEGER PRIMARY KEY` with no sequence. Fixed live and in `create-jobs-schema-extensions.sql` this session.
5. **Return Authorisations does not belong "with the rest of job-management" after all — it's barely coupled to jobs.** Real data: only 126 of 1,323 RAs (9.5%) reference a job at all; 60.7% originate from stock, 28.4% from a counter return. The Job-linked RA case is just a customer-lookup convenience, not a workflow dependency. RA's actual integration point is a POS-side stock/serial-audit transaction (`POS_GoodsReturned`) that already has a matching Postgres schema (`supplier_returns`, `supplier_return_line`, `serial_audit`, `serial_audit_trail` — all already modeled, no gap). **RA can be built as a standalone feature attached to Phase 2's POS/Void/Refund area, independent of and not blocked on the rest of Phase 3.** It is functionally a POS satellite module that happens to store its rows in the jobs database for historical-schema reasons.
6. **"Customer job history" should not be a separate Job Tracking screen.** The legacy app already combines POS purchase history and job history into one hybrid screen (`frmCustHistory3.vb`). The right port target is a **new "Jobs" sub-tab on Phase 2's existing Customer screen** (`JMxPOS8/ViewModels/CustomerViewModel.cs`, which already has Invoices/Item Sales/Payments/Quotes sub-tabs) — not a standalone Job Tracking feature. Small, low-risk, and can be built any time (doesn't need to wait for the rest of Phase 3).
7. **"On-site/mobile job scheduling" isn't a scheduling feature.** It's a magic-string flag (`GoodsInCare='ON-SITE JOB;'`) plus a filtered list view using the job's ordinary `DatePromised` field — there is no calendar engine, no mobile app, nothing to build beyond a filtered query. Two *separate* integrations hang off that flag: Exchange/EWS calendar sync (real but optional, dying API — **recommend dropping outright**, see Phase 0.3) and a staff SMS reminder poller (small, reuses the SMS client already being built, worth keeping).
8. **`GoodsInCare` isn't free text — it's a delimited multi-item collection** (type/brand/model/serial per item, up to several items per job, packed into one 250-char column via legacy encode/decode logic). The current ported schema copied the flat-column shape faithfully but not the actual semantics. This needs an explicit decision before building the intake/goods-in-care UI — see Phase 0.4.
9. **One real schema gap that blocks a feature, not just cosmetic drift**: `service_model_checklists` was ported with the wrong shape entirely (invented `ModelName`/`ItemOrder` columns instead of the real `RMStockId`/`TaskDescription` link) — blocks porting the service-checklist-template feature as-is. (`returnauthorizations`'s missing `RA_Symptoms`/`RA_DateGoodsReceivedBack`/`RA_ReturnResultComment` columns, flagged here in the original audit, were actually added and fully wired up the same session, in the RA feature build below - this bullet point was never reconciled with that. Confirmed 2026-09-02: `ra_symptoms`/`date_goods_received_back`/`return_result_comment` all exist and are read/written by `ReturnAuthorizationService`/`ReturnAuthorizationViewModel`. Nothing left to do here.)
10. **The legacy job-reporting module uses SQL Server's proprietary `SHAPE` syntax and a T-SQL scalar function** — neither has a Postgres equivalent. This is a real rewrite (two queries + in-app grouping, move the scalar-function logic into C#), not a mechanical port — flagged so it isn't underestimated.
11. **RA attachment blob content was never actually migrated** — metadata only (title/size/format), `doc_file_content` is `NULL` on all 8 RA attachment rows and all 142 job document rows. Whether the real bytes are recoverable from the old SQL Server backup is an open question worth resolving before promising "view old attachments" as a Phase 3 feature.
12. **The Job Tracking app never had its own database connection historically — confirmed architecturally significant.** The whole legacy suite ran one process against one shared SQL Server database via one shared connection; `CustomerBarcode` on a job was always just a lookup key into the same database, never a real cross-database reference. (A partial, never-finished dual-connection retrofit exists in the legacy VB.NET source from an earlier session — `DatabaseConfig.vb`/`modDatabaseAbstraction.vb` — confirmed inconsistent/inert; treat as noise, not a foundation.)
13. **Resolved, decisively: JobMatix is going back to being one single app, matching its actual history.** JobMatix was originally built as Job Tracking software; RA was added next; POS was added last (replacing MYOB Retail Manager) — it was never three separate products, that split only happened when this port work began. Per direction from the project owner, this port undoes that split: POS, Job Tracking, and RA become one Avalonia application, and — since a single app talking to two databases just recreates the cross-database problem in a new form — **the databases were merged too**. `jobmatix_jobs`'s 17 tables (26,383 jobs, 30,888 parts, 46,571 tasks, 1,323 RAs, and the rest) were merged into `jobmatix_pos` this session (full `pg_dump` backups of both databases taken first, kept in `db-backups/`; the standalone `jobmatix_jobs` database was left in place, untouched, as an extra safety net rather than dropped). This finally allows the real foreign keys that were never possible across two databases: `jobs.rmcustomer_id → customer`, `jobs.{rcvd,tech,delivered}rmstaff_id → staff`, `parts.stock_id → stock`, `parts.serviced_by_staff_id → staff`, `tasks.performed_by_staff_id → staff`, `returnauthorizations.{rm_stock_id→stock, supplier_id→supplier, staff_id_created/updated→staff}` — 14 new FKs added, all nullable (`ON DELETE SET NULL`), after nulling out a small number of historical rows (≤5% of any given column) whose legacy numeric ID didn't resolve to a real row. `returnauthorizations.goods_id`/`order_id` were deliberately left without FKs — ~74% orphaned, because the POS goods-received integration was added late in the legacy product's life and most historical RAs predate it. **This closes out Phase 0.4's cross-database question entirely** — there is no cross-database problem to solve anymore, Job Tracking/RA screens will just query the same connection JMxPOS8 already has open. It also means the `JobMatix62.Net` launcher's whole reason to exist (pick between two separate apps/processes) goes away — Job Tracking and RA become new tabs in the same window, not a separately launched app.

---

## Phase 0: Architecture Decisions — do this before more Phase 2/3 work

### 0.1 Store/location data model

*(unchanged from v2 — see `ROADMAP-ARCHIVE-2026-08.md` if you need the full option comparison)*. **Recommendation stands: Option A**, separate Postgres per store + a small central reporting API later (Phase 4). Schema stays single-tenant per database.

### 0.2 Historical data from Precise PCs

**Resolved.** Real historical data was migrated for both POS and Jobs databases this session via the `LegacyDataImport` tool (14,899 stock items, 11,129 customers, 39,884 invoices, 26,383 jobs, 30,888 parts, 46,571 tasks, 1,323 return authorisations, and more). This is no longer an open question.

### 0.3 Feature calls needed before building

- **Recurring "Subscriptions" billing** — still open. Does any current IBG store actually need recurring billing? If not, drop it.
- **Exchange/calendar sync** (`clsExchange20.vb`) — **now recommend dropping outright**, stronger than v2's hedge. Confirmed: it was optional even in the legacy app (silently skipped if unconfigured), fails gracefully, uses a dying API (EWS retirement), and provides no value the new stack couldn't get more cheaply from a modern calendar integration later if ever actually requested. The SMS-based staff reminder that shares the same "on-site" flag is unrelated and worth keeping — don't drop that too.

### 0.4 New Job Tracking architecture decisions (found during the Phase 3 audit)

These weren't visible until the legacy code was actually read. The cross-database access question this section originally posed is now moot — see "What Changed" #13: POS, Job Tracking, and RA are becoming one single app over one single database (`jobmatix_pos`), matching JobMatix's original history, so there's no second connection or API boundary to design. What's left to decide:

- **`GoodsInCare` schema.** Keep the legacy flat-text encoding (fast to ship, matches 26k historical rows as-is, needs a decode step to display), or redesign as a proper `job_goods_items(job_id, goods_type, brand, model, serial_no)` child table (matches what the data actually represents, requires a one-time backfill parse of history using the same decode logic already identified in the legacy code). **Recommend the child-table redesign** — it's a one-time migration cost against 26k rows, done once, versus re-implementing string encode/decode indefinitely. Defer to a fast-follow after a v1 slice ships with the flat field if you want to see the core workflow running sooner.
- ✅ **`servicemodelchecklists` schema fix** — done 2026-09-02 (`sql-scripts/fix-servicemodelchecklists-schema.sql`). Confirmed zero C# code referenced the old shape anywhere before touching it. `checklistitem` renamed to `task_description`; `rm_stock_id` (already added in a later extensions script, already populated on all 21 real rows) made `NOT NULL`; the unused `modelname`/`itemorder` columns (always blank/0 on real data) dropped. This only fixes the schema shape - the checklist-template editing UI and the per-job completion gate that depends on it are still real, separate, not-yet-scheduled work (investigated 2026-09-02: the legacy gate is a hard block keyed by per-service-item checklist completion, not a simple "has tasks/parts" check - there's no checklist data anywhere yet for a gate to inspect, so this alone doesn't unblock the "checklist-complete gate" item below).
- ~~**`returnauthorizations` missing columns**~~ — ✅ already done, same session as the RA feature build (2026-08-31) - stale by the time this file was next touched. See "What Changed" #9 above.
- **Attachment/blob storage pattern.** Both RA attachments and job documents need a file-storage answer at some point (photos of damaged goods, supplier paperwork, job photos). Real usage is low-volume (8 RA rows, 142 job rows historically) — **recommend a simple `BYTEA` column in Postgres**, matching the legacy approach directly, rather than standing up S3/object storage for this alone. Decide once, reuse for both RA and Job documents rather than solving it twice.
- **Old attachment content recovery** — separately, check whether the actual file bytes for the 150 existing attachment rows (currently metadata-only, `NULL` content) are recoverable from the old SQL Server backup, if "view historical attachments" matters to you. Not a blocker for new attachments going forward either way.

---

## Phase 1: Infrastructure & Database — done

**Done:**
- ✅ Docker Postgres 15 + pgAdmin running (5433/5050)
- ✅ **`jobmatix_jobs` merged into `jobmatix_pos`** — JobMatix is now one app over one database, see "What Changed" #13. `jobmatix_pos` is the single source of truth for POS, Job Tracking, and RA data going forward. Pre-merge backups of both databases kept in `db-backups/`.
- ✅ Foreign keys added to the Jobs tables (was flagged as a gap in v1/v2 — confirmed already fixed via `create-jobs-schema-extensions.sql` before this session; 14 more cross-domain FKs added this session as part of the merge, now that jobs/RA and customer/stock/staff/supplier live in the same database)
- ✅ Missing SERIAL sequences fixed across all known instances — 17 tables from the original `jobmatix_pos` (Phase 2 work) + 4 tables from the merged-in jobs tables (`quote_job_parts`, `model_checklist`, `ra_attachments`, `job_service_checklists`, fixed this session)
- ✅ `stock.requiresserial` schema drift reconciled

**Still open:**
- ✅ **Hash staff passwords** — fixed 2026-09-02, see that session's write-up above. Investigated first rather than assumed: the field is genuinely vestigial (barcode entry is this app's real identity mechanism; all 45 real staff rows had `password = ''`, nothing anywhere compares against it), hashed anyway so it's safe the day it does become load-bearing.
- ✅ **Encrypt `jobs.username`/`jobs.userpassword`** — done 2026-09-02, see that session's write-up above. AES-256-GCM (`Services/CredentialEncryptor.cs`), reversible (unlike `staff.password`'s hash) since `JobDocumentPdfService.DrawUserLogonSection` needs the real plaintext back to print on the docket. All 7,593 real affected rows migrated in place, self-verified round-trip before commit, real pre-migration `pg_dump` backup taken and restore-tested first.
- 🔧 **`service_model_checklists` schema fix** — see Phase 0.4. (`returnauthorizations`'s equivalent gap is already fixed - see "What Changed" #9.)
- 🔧 **Define or drop `jobmatix_main` and `jobmatix_backup`** — still empty shells, still an open call.
- ✅ Fixed `Jobs.DatePromised` hardcoded default 2026-09-02 (column is now nullable, no default) — see that session's write-up above. Existing rows already at the old 2020-12-25/2050-12-25 sentinels were deliberately left as-is.
- 🗑️ **Drop `JMxRetailHost620.Net`** and **`JMxKeyGen420_OS`** — unchanged from v2, still the right call.

---

## Phase 2: POS Application (JMxPOS8) — feature-complete for what's unblocked

**Done (this session, all verified against live migrated data):**
- ✅ Core services, full MVVM, multi-document Sale tabs, staff admin (manager-override pattern), complete sale workflow, stock/customer CRUD, reporting.
- ✅ Cash-up/EOD reconciliation.
- ✅ Stocktake (physical inventory count/reconciliation).
- ✅ Goods Received (supplier stock receiving workflow).
- ✅ Customer Statement report.
- ✅ Transaction lookup/void.

**Everything else in Phase 2 is blocked on a decision, not on effort:**
- **Receipt printing / cash drawer kick** — needs the CUPS/ESC-POS hardware architecture decided and test hardware in hand.
- **Subscriptions/recurring billing** — needs Phase 0.3's business confirmation (does any store actually need it).
- **Email integration** (send invoices/statements) — needs an SMTP decision.
- **Serial number tracking UI polish** — lower priority, not blocking a store from running.

**New, small, low-risk additions surfaced by the Phase 3 audit — can be picked up any time, don't need to wait for the rest of Phase 3:**
- **Customer "Jobs" sub-tab** — extend the existing Customer screen's Invoices/Item Sales/Payments/Quotes sub-tabs with a Jobs history tab (see "What Changed" #6). Now a plain same-database query (`jobs.rmcustomer_id` has a real FK to `customer` since the merge) — no cross-database plumbing needed.
- **Return Authorisations** — can be built as a POS-adjacent feature now that it's confirmed not to depend on the rest of Job Tracking (see "What Changed" #5 and Phase 3 below for its own scope). Needs the `returnauthorizations` schema fix from Phase 0.4 first, and a decision on attachment storage if RA attachments matter to you.

**Hardware** — unchanged from v2, still start procurement now in parallel with software.

---

## Phase 3: JobMatix Main Application (job/repair tracking) — core built this session

The legacy app (`JMxJT620.NET`) is ~94,000 lines across 79 files. A full read-through this session (not just file listing) found it's more tractable than the LOC count suggests — most of the bulk is Designer-generated UI boilerplate and repetitive per-document print-layout code, not business logic.

### Must ship together (size: L, this is the real core) — ✅ done

Job intake, job status/maintenance workflow, and parts lookup/allocation were **not independently buildable** — they share the same optimistic-locking model and status transitions are gated by parts/tasks-complete checks. Built and verified against real data this session as a new Jobs tab in JMxPOS8, alongside the Customer Jobs sub-tab and Return Authorisations that were also completed:

- Job intake: customer barcode lookup (snapshot denormalized onto the job, matching legacy behavior), goods-in-care/brand/model as flat fields for v1 (the itemization redesign from Phase 0.4 is still a deferred fast-follow, not blocking), problem description + symptoms, priority, backup/warranty flags, staff attribution. Booking/On-Site intake modes weren't built (only the equivalent of legacy's default Check-In path) - low-risk to add later since they share the same save path.
- The real 11-state status workflow, including the optimistic-locking "InProcess" mechanism (selecting a job to view/edit flips it to a locked variant, releasing on deselect, re-applying after any transition performed while still viewing it) - verified this actually round-trips correctly across selection changes, not just on paper.
- Parts lookup/allocation with live price-drift detection against current stock pricing (the legacy `gbShowAllParts` repricing feature).
- Not yet built from the original "must ship together" scope: the checklist-complete gate before completing a job (v1 allows completing without checking for recorded tasks/parts, just a soft warning), and photo attachments at intake. **Investigated 2026-09-02**: the legacy gate (`ucChildJobMaint.vb`'s `msCheckFormComplete`) is a hard block, but it's keyed by per-service-item checklist completion, not "has tasks/parts been recorded" (that check was explicitly dropped by the legacy app's own later revision) - so it needs real checklist *data* to exist first. That's a bigger chain than it looks: the `servicemodelchecklists` schema fix (✅ done, see Phase 0.4 above) → a checklist-template editing feature (port of `frmModelEdit3.vb`, not built) → instantiating a template onto `job_service_checklists` when a service item is added to a job (that table exists, correctly shaped, but has zero C# references anywhere) → a check-off UI on the ticket → only then the gate itself. Not independently buildable by just tightening today's soft warning.

**Original status-table reference** (unchanged from the audit, still the vocabulary the build follows):

  | Code | Meaning |
  |---|---|
  | `05-WaitListed` | Booked, not yet checked in |
  | `10-Created` | Intake complete, awaiting work |
  | `20-Suspended` | Paused (parts/customer wait) |
  | `23-InProcessSusp` | Suspended job, **locked** for edit |
  | `30-Started` | Actively being worked |
  | `33-InProcess` | Started job, **locked** for edit |
  | `40-QA` | Quality-assurance review |
  | `43-InProcessQA` | QA job, **locked** for edit |
  | `50-Completed` | Servicing finished |
  | `70-Delivered` | Handed back to customer |
  | `97-Cancelled` | Cancelled |

  The `2x/3x/4x` "InProcess" variants are the optimistic-locking mechanism described above - preserved in the build, not simplified away.

### Can ship separately / deferred without blocking the core

- **Job docket/quote printing** — 🔧 in progress, PDF rendering started 2026-09-01. Confirmed to use plain `PrintDocument`/GDI+ drawing, no Crystal Reports/RDLC dependency. Six distinct document types (job labels, receipt, quote, new-job intake docket, service/delivery record, customer maintenance report — `clsPrintDocs3.vb`), mechanically similar.
  - **Business decision (2026-09-01)**: real physical printer/cash-drawer hardware output is explicitly **deferred, not stubbed** — real-world experience with services like Syncro's print integration (cash drawers routinely failing to open, prints failing outright) means that needs its own real evaluation before committing to an approach, not a rushed add-on bundled into this. In the meantime, every document renders as a real, complete PDF (opened with the OS's default viewer) so the actual content/layout can be reviewed now.
  - ✅ **New-job intake docket** ("Customer Service Agreement") done — `JobDocumentPdfService.cs`, using PDFsharp (chosen over QuestPDF specifically to avoid QuestPDF's revenue-based Community license cap, a real commercial-licensing risk not worth taking here). Verified by actually rendering PDFs against real jobs and opening them, not just checking file magic bytes — found and fixed a real bug this way: `goodsincare`'s legacy tab-delimited packed format (see Phase 0.4) rendered as run-together words ("BlackGENERIC") because PDFsharp doesn't render raw tabs as visible whitespace.
  - Needed a `PdfFontResolver` (PDFsharp 6.x has no font enumeration outside Windows GDI) — resolves to Liberation Sans/DejaVu on Linux, real Arial on Windows/macOS.
  - ✅ **Receipt** done 2026-09-02 - the 2nd of 6 document types. **Correction to this file's own earlier framing**: investigated the other 4 (excluding Quote, still blocked) before assuming they were "mechanical extensions" like this one - they're not, in different ways, and it would have been wrong to just start building them on that assumption. Job Labels needs a real new capability (a scannable barcode font/renderer - `PdfFontResolver` only resolves Arial-equivalent regular/bold today), not just a new layout. Service/Delivery Record needs genuinely new multi-page text-overflow pagination logic (PDFsharp has no GDI+-style `HasMorePages` auto-continuation - the legacy version's page-splitting has no equivalent here yet) plus has no data source at all for its "tasks completed" list (only a `TaskTypes` *lookup* table exists, not a per-job completion log). Customer Maintenance Report needs the same pagination work, and its distinguishing feature (an attached-photos column) is flat-out blocked on job-attachment/image storage not existing in this port at all yet (Phase 0.4/1's still-open attachment-blob question) - same category of blocker as Quote printing, not a mechanical extension. Receipt was the one that actually was tractable without a new capability or a blocked prerequisite: reads back the real persisted `invoice`/`invoice_lines` rows by `job_number` (a new `JobService.GetInvoiceForJobAsync`) rather than recomputing totals independently, so it can never disagree with what `CompleteJobAndInvoiceAsync` actually charged - only available once a job's been completed/invoiced, which is the correct gate (nothing real to show before that). Verified against real invoiced jobs and the not-yet-invoiced case.
  - **Still not built**: Job Labels, Service/Delivery Record, Customer Maintenance Report (all three need real new work per above, not just a new `Render*` method) and Quote (needs the underlying job-quote workflow built first — no "awaiting quote" job state exists yet in this port; the original caveat about extracting real cancel-on-requote logic bundled into the legacy quote-print code still applies once that workflow gets built).
  - The `JobRecord`/`JobService` fields the docket needed but the app didn't read yet (`CustomerCompany`, `Username`/`UserPassword`, `GoodsOther`) were added — `Username`/`UserPassword` print faithfully (matching legacy) but are only ever populated on historical migrated jobs; this port's own intake form doesn't collect them, so new jobs correctly omit that section entirely.
- **Brand/model reference data** — ✅ done. `GoodsTypes`/`Brands`/`Symptoms`/`TaskTypes` (all four, not just the three originally named here) are edited through one generic reusable `ReferenceDataViewModel`/`ReferenceListEditorView`, matching the legacy app's single parameterized `frmListEdit` form used for all of them - a new "Reference Data" tab with four sub-tabs, plus matching entries under the Jobs menu. Verified against live data (108/145/156/202 real rows) including the delete-blocked-by-FK path (tried deleting an in-use `tasktype_id`, confirmed it fails cleanly instead of crashing). Wired into job intake 2026-09-02 - see that session's write-up above.
- **Service-checklist templates** (`servicemodelchecklists`) — a real but secondary feature layered on top of core job tracking. The Phase 0.4 schema fix is ✅ done (2026-09-02), but that alone doesn't unblock this - still needs a template-editing UI, per-job instantiation onto `job_service_checklists`, and a check-off UI before it's usable (see the "checklist-complete gate" entry above for the full chain). Naturally deferred.
- **Goods-in-care itemization** — v1 can ship with the flat legacy-compatible field; the child-table redesign (Phase 0.4) is a fast-follow, not a blocker.

### Important, independently schedulable (not part of the "must ship together" core)

- **Return Authorisations** — ✅ done. Built as a new Return Auths tab: the real 7-state lifecycle (Created → RMA-Requested → RMA-Granted → GoodsSentToSupplier → GoodsCompleted/Refused/Cancelled), origin Job/Counter/Stock, and the POS-side integration (permanently decrements stock and marks the serial audit row RETURNED when goods ship to the supplier, plus a `supplier_returns` header/line) - verified against real stock and a real serialized item. Printing (3 internal document types - record slip, courier label, packing slip) wasn't built; there's no supplier-facing email/electronic submission in the legacy app anyway, it's all printed paperwork, so this is a clean deferral.
- **Customer "Jobs" sub-tab** — ✅ done, built on the existing Phase 2 Customer screen rather than as a separate view (see "What Changed" #6).
- **SMS notifications** — ✅ done, DirectSMS only. All 4 legacy-supported gateways were built and verified first (via a fake HTTP handler, since no real credentials exist and a live send costs money/texts a real number), but checking the restored legacy SQL Server database (`JobTracking.SystemInfo` on the `jobmatix-mssql-restore` container) showed only DirectSMS was ever actually configured in production (`SmsGatewayHostName='directSMS'`, real Precise-PCs-era credentials on file) — the other 3 were evidently tried early on and abandoned. Simplified down to DirectSMS only rather than carrying 3 dead options forward. Settings live in the existing `systeminfo` key/value table (Staff tab, admin-gated); "Notify Customer" on the Jobs tab sends and logs onto `jobs.notifications`, matching the legacy append-to-job-record pattern. The real historical password was deliberately not copied anywhere (live credential for a paid third-party account under the old business name) - enter it directly in the Staff tab if you want to reuse that account, or set up a fresh one.
- **Email notifications (SMTP)** — ✅ done. Same pattern as SMS: real historical config confirmed in the restored legacy database (Mailgun relay, smtp.mailgun.org:587), settings panel next to SMS on the Staff tab, "Send Email" alongside "Send SMS" on the Jobs tab's Notify Customer panel (looks up the linked customer's on-file email via the real `rmcustomer_id` FK). Independent of the Exchange/EWS calendar integration (dropped, see Phase 0.3) — unrelated despite both being "email."
- **Job reporting** — ✅ done. 4 report types (Jobs/Parts/Staff/Timesheet), built into Phase 2's existing `ReportsViewModel` grid/summary pattern rather than porting the legacy GDI+ print-report renderer. The feared SQL Server `SHAPE`/scalar-function rewrite turned out to already be solved in the legacy VB app itself — its own `gCurComputeChargeableHours`/`gbQueryWorkSessions` parsed `jobs.sessiontimes` (a free-text session log) in application code; the dynamically-created T-SQL function just repeated that same parsing for use inside the SHAPE query. Ported that parsing to C# (`SessionTimesParser.cs`), verified against 2,000 real jobs at 99.95% agreement with each job's own recorded `totalservicetime`. The SHAPE parent/child structure became a plain query plus a JOIN (Parts report) or in-app grouping (Jobs/Staff reports) — no Postgres SHAPE equivalent needed at all. Labour hourly rates (per-priority) recovered from the restored legacy SQL Server database and seeded into `systeminfo` (real values: 110/135/185, not invented). All four verified against the full real dataset (25,532 jobs, 30,017 part lines, 87 staff, 4,711 timesheet sessions). Found a real legacy data anomaly while spot-checking (one part named "Not in System Yet" priced at $100,000,000) — confirmed as pre-existing dirty data, not a bug, and left as-is rather than silently filtered.
- **On-site job list + staff SMS reminder** — ✅ done 2026-09-02, see that session's write-up above. Not a scheduling engine (see "What Changed" #7).

### Explicitly dropped

- MYOB Retail Manager quote import (discontinued product, no modern equivalent needed — JMxPOS8 is the quote/order source going forward).
- **Exchange/EWS calendar sync** — now a firm recommendation to drop (see Phase 0.3), not just a hedge.

### No longer needed at all

- `JobMatix62.Net` launcher — under v2 this was scoped as "needs a small replacement" (a POS/Job-Tracking picker). That's now moot: per "What Changed" #13, JobMatix is going back to being one single app, not two apps a launcher picks between. Job Tracking and RA are new tabs in the same JMxPOS8 window (matching the existing Sale/Stock/Customers/Staff/Reports/Transactions tabs), not a separately launched module. Nothing to build here.

### UI/UX notes (2026-09-01, direct feedback from running the live app) — ✅ done

The business owner ran the app and flagged the top tab strip as cluttered and poorly labelled (11 tabs, one row wrapping), plus real gaps found while looking at the Tickets (formerly "Jobs") tab specifically:

- **Navigation**: tab strip reduced to Sale/Stock/Customers/Tickets only. Staff, Reports, Transaction Lookup, Stocktake, Intake (renamed from Goods Received), Returns, and Reference Data are unchanged as actual tabs (`SelectedTabIndex` navigation, all existing menu commands) but no longer shown as strip headers — a new `TabItem.navhidden` style (`IsVisible=False`) hides the header button while leaving `SelectedIndex`-based selection and content working normally. Reached via the menu bar instead (added the one missing menu item, Transaction Lookup, under Sales).
- **Renamed "Jobs" to "Tickets"** throughout the UI (tab, menu, "Create Job" → "Create Ticket", the Customer screen's Jobs sub-tab) — UI copy only, the underlying `JobViewModel`/`JobRecord`/`JobService` names are deliberately unchanged.
- **Restored the legacy tree-style ticket list** (status groups — Waitlisted/New/Suspended/In Progress/QA/Completed — each with a count, expand/collapse) that this port had flattened into one flat list. Rebuilt live from `OpenJobs` whenever it changes.
- **Ticket search** (`JobService.SearchJobsAsync`) — by exact ticket number or partial customer/problem match, across every status including delivered/cancelled. There was previously no way to find a ticket that wasn't already in the open list at all.
- **Notes section** added to the ticket detail panel (Problem Details/Diagnosis/Service Notes) — these were being captured (at intake and at completion) but never shown again once you navigated away from the ticket.
- **Click-through from Customer → Tickets sub-tab**: the ticket number is now a clickable link that switches to the Tickets tab and opens that exact ticket (`JobViewModel.OpenJobByIdAsync`, new — works for any status, not just open ones, since a customer's ticket history is mostly closed/delivered/cancelled tickets).

Verified by actually running the app and screenshotting each piece against real data, including opening a delivered ticket through the new cross-navigation path to confirm the existing optimistic-lock logic (`HandleSelectionChangeAsync`) degrades safely outside the open-jobs list (it already did — the lock transition only applies to Suspended/Started/QA statuses).

**Follow-up round (2026-09-01, same day)** — feedback after reviewing the round above live:
- Avalonia's built-in `Expander` wasn't resolving its chevron icon resource in this environment, rendering its literal internal theme key (`Avalonia.Controls|Expander:down`) as visible text with a stray gap. Replaced with a plain `Button` + a hand-drawn glyph (`JobStatusGroup.ExpandGlyph`) — sidesteps the broken resource entirely.
- No way to close an opened ticket and get back to the list — added a "Back to list" button (`CloseTicketCommand`). Also found in the process: the detail panel's `Grid.Row` was never bumped when the search row was added earlier that session, so it was silently overlapping the list instead of sitting below it.
- Viewing a ticket now takes over the **full tab width** instead of a cramped side column (`JobViewModel.IsViewingTicket`) — list mode (New Ticket form + search/grouped list) and detail mode are mutually exclusive full-width sections now, not a fixed two-column split, so the New Ticket form no longer eats space while a ticket is open.
- New `job_notes` table/`JobNote` model — a real running log of notes (unlike the single-value legacy `servicenotes`/`diagnosis` columns), each flagged public or private and color-coded (amber vs. green) so the distinction reads at a glance. There was nowhere to add a new note at all before this.
- Note entry is a proper multi-line textarea now (real notes can be "a wall of text"), and the whole detail panel is wrapped in a `ScrollViewer` — Parts and everything below it could run off the bottom of the tab with no way to reach it.
- Service notes completed via "Complete" now carry the same staff-name + timestamp attribution real historical data already has (e.g. "Stewart: 12-Nov-2016  13:00") — the completion flow never wrote that prefix, so every ticket completed through this port so far had unattributed notes while migrated ones didn't.

**Second follow-up round (2026-09-01)** — smaller polish items plus a real workflow change:
- Email subject box was centering instead of left-aligning (a `Stretch`-aligned control with an explicit `Width` centers within available space by default in a vertical `StackPanel`) — fixed with explicit `HorizontalAlignment="Left"`.
- Scrollbar sat flush against note/notify text — gave the detail panel's content a small right margin.
- `GetOpenJobsAsync`'s 200-row cap was silently hiding real jobs — only 300 jobs are genuinely "open" (not delivered/cancelled) across the full 26k+ job dataset, and 200 sat in the middle of that, quietly cutting ~100 real jobs out of every status group (Completed showed 124 instead of ~216). Raised to 2000.
- **Status change unified into one dropdown** ("valid moves only", confirmed with the business owner over two other options — a free-form any-status override, and keeping the old buttons) — replaces the Start Work/Suspend/Send to QA/Back to Work/Deliver/Cancel button row with a single `AvailableStatusOptions` dropdown + one "Change Status" button. Same underlying methods, same validation, same side effects (e.g. Start Work still needs a staff barcode) - this only consolidates the trigger. Verified against three real jobs (Created/Started/Delivered) that the option list is exactly right for each, including that a terminal ticket correctly offers nothing.
- **Found in the process, not yet fixed**: `JobViewModel.OnSelectedJobChanged` applies/releases the optimistic "InProcess" lock via a fire-and-forget task (`_ = HandleSelectionChangeAsync(...)`), not an awaited one. In the long-lived GUI app this is imperceptible, but a short-lived process (confirmed with a console test) can exit before that background task finishes, so it's possible in principle for a lock apply/release to lose a race against something else touching the same job in a tight window. Low real-world likelihood (needs near-simultaneous access to the same ticket), not urgent, but a real architectural sharp edge worth knowing about before relying on the lock for anything stronger than "avoid two staff editing the same ticket by accident."

**Third follow-up round (2026-09-01)** — misclick safety, serial scanning, and ticket time tracking:
- Delete buttons for Customers and Stock committed on a single click - a misclick could delete either outright. Both now use a two-step arm/confirm pattern (`IsDeleteArmed`, `ArmDelete*Command`/`Delete*Command`): first click turns the button into a red "Confirm Delete?", second click actually deletes; selecting a different row resets the arm. No modal dialogs exist anywhere in this app, so this matches the app's existing interaction style rather than introducing a new one.
- Scanning a serial number (rather than a product barcode) into the Sale screen's barcode/product field now resolves the product and fills in the serial field automatically (`SerialService.FindStockIdBySerialAsync`, falls back only when the barcode lookup itself misses) - previously only a product barcode worked there, and a scanned serial fell through to the plain-text description search.
- **Ticket time tracking** ("a start stop auto add time to notes and billing was a major sore point" of the legacy app) - built concurrent by design: a running timer is just a `job_time_entries` row with `end_time IS NULL`, so any number of tickets can each have their own running timer at once (a tech bouncing between an install, a screen swap, and a new build), with no separate "active timer" state to keep in sync and no restriction to "one timer at a time" that would have forced an artificial choice between concurrent and single-focus usage. Per-ticket Start/Stop control shows a live elapsed display; stopping auto-logs a private note summarizing the duration and what was done (typed in an optional field first) - matching the ask to "auto add time to notes." A status-bar indicator ("N timers running"), visible from any tab, jumps to Tickets and shows a filtered list of every ticket with a timer currently running, reusing the existing ticket-search results view rather than a new UI surface. Billing/invoicing integration is deliberately not wired up yet - this only tracks the time itself.
  - **Two real bugs found and fixed while building this, both worth remembering**: (1) Avalonia's `!` binding negation only works on `bool` - binding `IsVisible` to a nullable object (`!CurrentJobTimer`) silently fails the type conversion and leaves *both* the "no timer" and "timer running" panels visible at once instead of toggling between them. Fixed by adding an explicit `HasCurrentJobTimer` bool property, the same pattern already used for `HasRunningTimers`/`IsCashupView` elsewhere. (2) The dev DB container's clock runs UTC while the app runs in AEST (UTC+10) - letting `start_time`/`end_time` default to Postgres's own `CURRENT_TIMESTAMP` made every timer appear to have already been running for ~10 hours the instant it started (confirmed live: a timer showed `10:00:11` elapsed seconds after starting). Fixed by stamping both columns with an app-supplied `DateTime.Now` instead, matching `StocktakeService`'s existing `@now` override for the identical reason. **The same bug was also found and fixed in `JobService.AddJobNoteAsync`**, added earlier this session - note timestamps were silently off by the same ~10 hours (confirmed live: a note added at 13:49 local showed "03:48"). Any *other* code added this session that lets a timestamp default to the DB's `CURRENT_TIMESTAMP` and later compares or displays it against `DateTime.Now` should be treated as suspect until checked.
  - Also fixed in passing while screenshotting this: the ticket-search results row's job-number column was a fixed 50px wide, too narrow for a 5-digit ticket number, so it visually ran into the description text (`#3236` + `3Jocelyn...`) - widened to `Auto` with a margin.

**Fourth follow-up round (2026-09-01)** — Reports page rework, direct feedback:
- The date-range pickers and the Customer Statement's barcode field used to show unconditionally above *every* report, doing nothing for the ones that ignore them. Now `ReportsViewModel.ShowDateRange`/`ShowCustomerBarcode` gate their visibility per report (date range hides for Stock Value/Low Stock/Customer Accounts/Cash Up, which never read `StartDate`/`EndDate`; the barcode field only shows once Customer Statement is the active report). Customer Statement itself is now a normal button in the report-type row (previously it was a bare barcode box + button with no equivalent to the other reports' selection button) so it can both set itself as the active report immediately - revealing its own barcode field - and get the same active-state highlight as everything else.
- Cash Up was hardcoded green at all times, so it kept looking "selected" after switching to a different report. Every report button now highlights only when it's actually the active one (`ReportTitle` doubles as the unique key - one `IsXSelected` bool per report plus a shared `Button.reportBtn.active` style, the same `.active` class convention already used for the Sale tab strip).
- Report grid columns showed generic "Column 1..4" headers regardless of what was actually in them, and had no sorting. Each `Run*Report` method now sets descriptive headers (e.g. Stock Value: Code/Description/Qty/Cost Value) via `Column1Header`.."Column4Header`, and the header row is now a set of clickable sort buttons (`SortByColumnCommand`) with a `▲`/`▼` indicator, using a numeric/date-aware comparer (`ReportColumnComparer`) so formatted columns like "$1,234.56" sort by value, not lexicographically.

**Fifth follow-up round (2026-09-01)** — color palette modernization and a restored legacy approval field:
- The timer box and the Problem Details/Diagnosis/Service Notes boxes used a pale yellow/cream palette that read as dated ("i'm not a fan of the yellow and orange boxes for timers and ticket information"). Recolored to a two-tone blue system: light blue (`#E3F2FD`/`#90CAF9`) for informational/neutral chrome (timer box, the three info boxes, the status-bar running-timers indicator), darker blue (`#BBDEFB`/`#1976D2`) for private notes (was amber/orange) - keeping private clearly distinct from public notes' existing green, with no yellow or orange left anywhere in the ticket UI.
- **Restored the legacy "Customer Instruction" approval field**, dropped entirely by this port. In the legacy app (`ucChildNewJob.vb`'s `optQuotation` radio group) every new job required selecting one of Quotation Required / Proceed with Service / Proceed only to Cost Limit before it could be created - baked into `problemshort` as a boilerplate marker string (`*PROCEED-WITH-SERVICE*;` etc.), which is exactly why that field is so often *just* the marker with no real description (confirmed against real data: 15,479 of ~26k jobs are pure `*PROCEED-WITH-SERVICE*;`, 6,388 `*QUOTATION-REQUIRED*;`, 371 `*PROCEED-TO-LIMIT*;`). This port had no control for it at all - direct feedback: "there is a proceed with service note on all jobs which was supposed to indicate someone selected proceed with service... to indicate the work was approved and we should start it." Rebuilt as a required dropdown on ticket intake (`JobViewModel.NewCustomerInstruction`, validated exactly like the legacy form: "Customer Instruction must be selected"), stored in the same marker format for compatibility with already-migrated jobs, and shown read-only as a color-coded banner on the ticket detail page (red/coral = Quotation Required, green = Proceed with Service, blue = Proceed to Limit - matching the legacy main-job-page's own `labInstructions` banner concept, colors modernized to fit the new palette above).
  - **Found and fixed in the process**: the ticket detail header (`JobRecord.Summary`) built itself from the raw `ProblemShort` field, so a job whose entire `problemshort` was just the marker literally showed "*PROCEED-WITH-SERVICE*;" as its description in the header - confirmed live on job #32363. Also, `ProblemDescriptionHelper.Summarize`'s old boilerplate check only matched when `problemShort` was *exactly* one of the three marker strings, so a job with a real description **and** the marker mixed together (the normal case, since the legacy form appends the marker to whatever was typed) would show both concatenated instead of stripping the marker - a latent bug that predates this session's own `ProblemDescriptionHelper`, only surfaced now while wiring up the new marker-aware helpers (`ExtractCustomerInstruction`/`StripCustomerInstructionMarker`). Both fixed together; `Summary` now uses the same cleaned text as list-view `DisplaySummary`.
  - Also fixed in passing: the same fixed-50px job-number column width bug (found and fixed in the search-results list two rounds ago) was still present in the default grouped ticket list's own separate item template - widened there too.

**Sixth follow-up round (2026-09-01)** — status workflow gaps, a hidden timer requirement, and two real report data bugs:
- **Start Timer "did nothing"**: it wasn't silent - clicking it without a staff barcode set a real status message, but that message only rendered far down the page (near Parts/Notify Customer), invisible without scrolling past the whole Notes section. The timer widget also had no staff-barcode field of its own at all, relying entirely on the shared field near Change Status - so a user opening a ticket and immediately clicking Start Timer at the top had no visible path to the field it needed. Added a dedicated staff-barcode box directly in the timer widget (same underlying `ActionStaffBarcode`, just also editable from here) and duplicated the status message inline in the timer box.
- **Status workflow gaps** - two real transitions were simply missing, not just mislabeled: Waitlisted (goods not yet physically arrived) had no way to move to New at all except Cancel - added `CheckInAsync`/"Check In (arrived - move to New)". Started/In Progress had no way back to New if started by mistake - added `ReturnToNewAsync`/"Move Back to New (undo Start)". Both follow the existing unified-dropdown pattern exactly (`JobService.RequireTransitionAsync`, same validation shape as every other transition).
- **Stock Value Report undercounted by ~95%** (showed 106 items, not "probably paginated" as guessed - the real count is 1,992): `GetAllStockAsync(1000)` applies its row-count limit in SQL, ordered by stockcode, *before* the report's `QuantityInStock > 0` filter runs client-side - so only the first 1,000 stockcodes alphabetically were ever considered, and only 106 of those happened to be in stock. Raised the fetch to 20,000 (comfortably covering all ~14,750 active rows) so the filter sees the real dataset. Confirmed post-fix: 1,992 items, matching a direct DB count exactly. This also fixed "sorting only sorts what's on the page" as a side effect - once the full result set loads into `ReportData`, `SortByColumnCommand` was always sorting the whole collection, not a page of it.
- **Low Stock Report always showed 0**, not a data problem - `GetAllStockAsync`'s SELECT never included `minstocklevel`/`reorderquantity` at all, so `StockItem.ReorderLevel`/`ReorderQuantity` silently stayed 0 for every row regardless of the real migrated values (confirmed ~2,100 stock rows have a real reorder level set). Added both columns to the query. Confirmed post-fix: 1,599 items, matching a direct DB count.
- **No trigger to re-run a report after changing the date range** - clicking a report's own button re-runs it, but that's not an obvious "reload" action once the button is already highlighted as the active report. Added a "Refresh" button next to the date range that re-dispatches to whichever report is currently showing.
- **Customer tab invoice linking** - Invoice # in the Invoices sub-tab, and a new small "Inv #___" link in Item Sales and Payments (only shown when a payment actually has one - not every payment is tied to an invoice), now jump to Transaction Lookup and load that exact invoice, mirroring the existing ticket-number link pattern. Reused the existing `TransactionLookupViewModel.SelectInvoiceByIdAsync` ("Show Last Invoice" already used this same lookup).
- **"Can't find any customers with quotes"** - not a bug: confirmed zero rows anywhere in the migrated `invoice` table have `transactiontype = 'QUOTE'` (36,023 `sale` + 2,143 `SALE` + 1,719 `refund`, nothing else). The Quotes sub-tab and its backing query (`GetCustomerQuotesAsync`) already exist and are correctly wired - there is simply no historical quote data to show, and the Sale screen's own "Quote" button doesn't yet have a real workflow behind it (see the Quote → Job → Invoice pipeline note below).

**Customer Instruction approval flow - ✅ done (2026-09-01)**: the intake-time-only design from the previous round was missing an "Approve" action, confirmed via a design-fork question - business owner picked keeping the 3-way intake dropdown as-is and adding a separate Approve action, then clarified the deeper reason why: **there are two distinct kinds of "quote" in this business**, and the Customer Instruction field is really only about the second one. (1) A customer wants a computer *built* - they approve a component list, the quote becomes a job with those parts already on it. (2) A customer brings in a *broken* item under a standard service charge - staff diagnose it, quote the repair cost, and the ticket sits at "Quotation Required" until the customer calls back to approve it. Only case (2) is what Customer Instruction models, and it was missing the "customer called back and approved" action - added `JobService.ApproveQuoteAsync` (rewrites the problemshort marker from `*QUOTATION-REQUIRED*;` to `*PROCEED-WITH-SERVICE*;` in place, same helper functions as everything else in this area) plus an "Approve" button that appears on the instruction banner only while a ticket is at Quotation Required. Verified live: created a test ticket at Quotation Required, confirmed the Approve button appears, clicked it, confirmed the banner flips to green "Approved - Proceed with Service" and the button disappears.

**Corrected 2026-09-01 (later the same day) - the build-quote pipeline for case (1) already has real historical data, I'd looked in the wrong place**: direct feedback questioned why "quotes aren't populating," specifically for customer 10000 ("used specifically for quoting"). My earlier claim above - that zero quote data exists anywhere in the migrated data - was only true for the `invoice` table. The real data lives in a completely separate legacy table, `QuoteJobParts` → migrated as `quote_job_parts` (7,842 rows, FK to `jobs.job_id`, columns: barcode/description/cat1/cat2/orderqty/stockid/sell_inc/orderid), which the app has never read since the migration. Customer 10000 (barcode) → customer_id 3139, literally named **"QUOTES, Customer"** in the legacy data, has 156 real historical build-quote jobs this way (34 Completed, 333 Delivered). The legacy mechanism had no separate "quote" object at all - a build quote was just an ordinary Job (under that shared placeholder account) with a `quote_job_parts` list attached, converted to a *real* job manually by staff re-keying everything once a customer committed - no automatic conversion ever existed.

**Full plan (approved, not yet built)** - reuses `quote_job_parts` for new tickets going forward under the *real* customer (no placeholder-account workaround needed) and automates the manual step the legacy system never had:
1. **Serial capture on real job Parts** - `parts.serial_number` already exists in the schema, unused by any C# code. Wire it up mirroring `SaleService.AddItemByBarcodeAsync`'s existing `RequiresSerial` enforcement pattern; block `Complete()` until every part needing one has it.
2. **Quote Parts** - any ticket at Customer Instruction "Quotation Required" can have proposed components added (a repair-quote ticket just won't have any; a build-quote ticket will), written to `quote_job_parts` with an editable proposed price. Also surfaces the 7,842 rows of existing historical data, currently invisible everywhere in the app.
3. **Approve auto-converts** quote parts into real `parts` rows automatically, locking in the quoted price - "the quote is converted to a job with all components added to the job automatically," the owner's own words. Extends `ApproveQuoteAsync` from the round above.
4. **Job → Invoice generation on completion** - a new `JobService.CompleteJobAndInvoiceAsync`, modeled tightly on `SaleService.CommitSaleAsync`'s proven transaction shape (same tax/serial/stock-movement logic, `transactiontype = 'SALE'` so it appears in existing reports, idempotent via the already-present-but-unused `invoice.job_number` column plus a new partial unique index). One shared "Labour" stock row (billable `job_time_entries` hours × priority rate) and one "Non-Stock/Miscellaneous Part" row cover the two cases that don't map to a normal stock-backed line. `is_warranty_part` is left as a plain pass-through (real supplier-warranty handling already goes through the separate RA subsystem, confirmed with the owner - out of scope here).

Full design detail (transaction boundaries, field mappings, landmines considered) is in the session's implementation plan; see this file's own commit history / the follow-up round entries below for what's actually shipped vs. still pending as phases land.

**Seventh follow-up round (2026-09-01)** — four real bugs found while investigating the quote question, all fixed and verified before starting the pipeline build:
- **"Commit Sale does nothing"** - not silent, but close to it: `SaleTabView`'s staff-number field only resolved into `CurrentStaff` on the Enter key (matching a real barcode scanner's trailing Enter), with no `LostFocus` fallback - so typing a staff number by hand and tabbing/clicking away (rather than pressing Enter) left `CommitSale()` silently no-op'ing on `CurrentStaff == null`. This explained every symptom reported in one go: Commit doing nothing, Hold correctly refusing because a payment *had* been separately applied, and Reports/Cash Up showing nothing because no sale had actually committed all session. Added a `LostFocus` handler alongside the existing Enter handler. Verified live end-to-end: scanned an item, resolved staff via the new path, committed a real invoice, confirmed it in the DB, then cleaned up the test data.
- **Transaction Lookup showed "VOIDED" on almost every transaction** - a real data-trust bug, not cosmetic: `IsVisible` was bound to "Status is non-empty" instead of "Status equals VOIDED." Real data: 39,884 invoices are `Completed`, only 1 is genuinely voided - but every non-empty status rendered the red badge. Confirmed live against a real customer (all 15 of their invoices are `Completed`, the UI showed all 15 as VOIDED). Fixed with an `IsVoided` bool property, same pattern as this session's other `Is*`/`Has*` Avalonia-binding-gotcha fixes.
- **Transaction Lookup's Type/Period dropdowns didn't work at all** - `SelectedItem` was bound directly to a plain string while the box's actual items were inline `ComboBoxItem` children, which never match in either direction (confirmed by a live screenshot: both dropdowns rendered blank despite non-empty ViewModel defaults). This is why "search invoices/payments/quotes" did nothing - changing the Type never actually changed `LookupType`. Fixed by switching both to `ItemsSource` against a real string array, the same pattern the Customer Instruction dropdown already used correctly. The identical broken pattern was also found (via grep, not yet reported as symptomatic) in three other places and fixed at the same time: Return Authorisation's Origin and Return Result pickers, and the ticket-intake Priority picker.
- Also found while chasing this, **not yet fixed, flagged for later**: filtering Transaction Lookup by a customer barcode doesn't appear to actually narrow results in one live check (multiple different customers' names appeared in a supposedly customer-filtered list) - the underlying SQL, checked directly, filters correctly, so this looks like either a UI-level race or something specific to how the search was triggered rather than a broken query; worth a clean re-check before trusting customer-barcode search results.

**Quote → Job → Invoice pipeline, Phase 1 - ✅ done (2026-09-01): serial number capture on real job Parts.** `parts.serial_number` existed in the schema, untouched by any C# code. Wired up end-to-end, mirroring `SaleService.AddItemByBarcodeAsync`'s existing `stock.RequiresSerial` enforcement exactly: `JobService.AddPartByBarcodeAsync` gained a `SerialRequired` result (checked before any INSERT, so re-submitting after filling in the serial is safe); the ticket page's Parts scan box shows a serial field only once a scanned item is found to need one (same two-step shape as the Sale screen's own serial prompt); each part row shows a "⚠ Needs serial" flag for any historical part that predates this feature; and `Complete()` now hard-blocks (not just warns) if any part still needs one, with the message echoed both in the timer box and directly under the Parts list - not just the far-bottom status line, after this session found two earlier bugs (Start Timer, Commit Sale) caused by exactly that kind of easy-to-miss placement.

**Real bug found while verifying this, not fixed - flagged as an amplification of an already-documented risk**: `JobViewModel.OnSelectedJobChanged`'s known fire-and-forget `HandleSelectionChangeAsync` call (documented earlier this session as a low-likelihood lock-race) is what actually loads the `Parts` collection - so a caller that opens a ticket and acts on `Parts` immediately afterward (proven live: my own test harness did exactly this) can race ahead of that load and see a stale/empty `Parts`, silently bypassing the serial-completion check above. A human clicking through the UI has enough natural reaction time that this is unlikely to bite in practice, but it means the new validation's safety isn't absolute - the underlying fire-and-forget pattern is the real fix needed, out of scope for this phase (matches the existing ROADMAP entry for the original finding).

**Quote → Job → Invoice pipeline, Phase 2 - ✅ done (2026-09-01): Quote Parts.** Reuses `quote_job_parts` (Phase 1's discovery) for new tickets going forward, not just as historical data - any ticket at Customer Instruction "Quotation Required" gets a "Quoted Components" section (scan-to-propose, editable quantity/price since a quote is a proposal, not a commitment yet) and it's shown whenever a ticket has any quote-parts at all, current or historical, so the 7,842 old rows stay visible on tickets that have long since moved past that status. Verified live: job #3047 (a real 2011 "QUOTES, Customer" build, previously completely invisible in the app) now correctly shows its 11 quoted components with real prices; a fresh test ticket at Quotation Required correctly showed the scan/add controls, added a real quote line, and the section correctly stayed hidden on ordinary tickets with nothing quoted.

**Quote → Job → Invoice pipeline, Phase 3 - ✅ done (2026-09-01): Approve auto-converts quote parts into real parts.** Extends `ApproveQuoteAsync` (built two rounds ago) - after flipping the Customer Instruction marker, any `quote_job_parts` rows on the job are converted into real `parts` rows automatically, "the quote is converted to a job with all components added to the job automatically" in the owner's own words. Locks in the *quoted* sell price (a commitment already shown to the customer) but re-pulls the *current* cost price from stock at conversion time, since `quote_job_parts` never captured cost at all - only a sell price - so a fresh cost figure is more accurate than none for margin reporting. The source quote rows are left in place afterward, not deleted, as a permanent record of what was quoted. Verified live end-to-end: a quoted USB adaptor (qty 2, quoted price $7.23) became a real committed Part with that exact price locked in and the correct current cost ($4.50) pulled fresh, while the original quote line stayed visible for the record.

**Quote → Job → Invoice pipeline, Phase 4 - ✅ done (2026-09-01): Job → Invoice generation on completion.** The last and largest phase - `JobService.CompleteJobAndInvoiceAsync` replaces the old bare status-flip `CompleteAsync`, reusing `SaleService.CommitSaleAsync`'s proven transaction shape (same tax/serial/stock-movement logic) rather than reinventing it, designed with a dedicated second-pass review before building. One transaction covers the status flip *and* the invoice/stock/serial writes - any failure (e.g. the idempotency check below) rolls back everything including the status change, so a job never gets stuck "completed but not invoiced." `transactiontype = 'SALE'` deliberately, so it's real revenue/COGS from day one in every existing report without teaching each one a second transaction type. `invoice.customer_id` falls back to the walk-in customer (job.RmCustomerId is nullable); `invoice.staff_id` is the *completing* staff (resolved from the barcode already required at Complete), not `job.TechRmStaffId`. Labour is one line per invoice summing every billable `job_time_entries` row for the job (hours × the job's priority rate, reusing `JobDocumentPdfService`'s existing rate lookup rather than duplicating it) against a new shared "Labour" stock row; a hand-typed part with no matched stock (`parts.stock_id` is nullable) routes to a second new "Non-Stock/Miscellaneous Part" row - both seeded in `sql-scripts/create-job-invoice-extensions.sql`, `is_non_stock_item = true`, neither one's own price ever read (line amounts are computed dynamically).

**Real finding while building this - the planned DB-level uniqueness guard had to be dropped**: `invoice.job_number` (an existing, unused column, chosen over adding a new `jobs.invoice_id` for the idempotency link) was meant to get a partial unique index as the real concurrency safety net for a double-click on Complete. Applying it live failed immediately - real migrated data has one genuine historical exception, job #18380, with two real 2019 invoices sharing that job_number (confirmed live; every other one of the 5,711 non-null values is unique). Altering real financial records to force a schema constraint through wasn't this session's call to make, so the index was dropped from the migration; `CompleteJobAndInvoiceAsync`'s in-transaction "does an invoice already reference this job" check is the *only* protection now, not a courtesy on top of a DB guarantee - documented as a known, accepted, low-likelihood limitation (small team, single-till workflow) rather than quietly pretending the original design shipped as planned.

Verified live end-to-end on a real test ticket (2 real parts, one requiring - and given - a serial, plus a synthetic 2-hour billable time entry, Priority H → $110/hr): Complete generated invoice #53950 correctly in every dimension checked - header totals (`subtotal`/`taxamount`/`total_inc` all hand-verified against the line figures), three lines (two parts with correct per-line cost/sell/margin math, one labour line reading "Labour (2 hrs)" at exactly $220), `stock.quantityinstock` decremented by 1 on both real parts, and the job status correctly landed at `50-Completed`. Then forced the job back to `40-QA` and called Complete again - correctly rejected with "Job #33391 has already been invoiced (invoice #53950)," and critically the job's status **stayed at 43-InProcessQA** (the failed attempt's status-flip rolled back cleanly, proving the one-transaction design actually holds). Finally confirmed the real invoice surfaces correctly in Transaction Lookup - right transaction type, right amount, right staff, correctly *not* flagged VOIDED (a nice incidental re-confirmation of this session's earlier VOIDED-badge fix, sitting right next to a genuinely-voided real invoice in the same list). All synthetic test data (job, parts, time entry, invoice, invoice lines) and stock quantities were cleaned up afterward.

**This closes out the Quote → Job → Invoice pipeline** first raised when investigating why "quotes aren't populating." All four phases are done: serial capture on Parts, proposing components on a Quotation Required ticket, automatic conversion on Approve, and now real invoice generation on completion.

---

## Phase 4: Cross-Store Reporting

*(unchanged from v2)* — genuinely new work, not a port of anything. Build the small central reporting API (Phase 0.1 decision) after Phase 2/3 are solid at a single store; each store's install must keep working standalone without it.

**Backups**: unchanged — rsnapshot + DO Spaces + `pg_dump` cron, not `backup-agent`.

---

## Phase 5: Testing, Pilot, Rollout

*(unchanged from v2)* — pilot at one store first, no live-cutover risk (greenfield), hardware should already be procured by this point.

---

## Phase 6: Serial-Level Costing (COGS/FIFO) & Central Multi-Store Catalog

Raised by the business owner 2026-09-01. Two related asks: real cost-of-goods-sold reporting (per-unit, not per-SKU), and a central place to control stock/pricing across stores, similar to how Fieldpine/Neto work today.

### 6.1 Per-unit cost tracking (the "COGS/FIFO" ask) — ✅ done

**The actual problem, confirmed in code**: `stock.costprice` is one field per SKU, overwritten on every goods-received transaction (`GoodsReceivedService.cs` — "latest cost wins"). So a product that came in at two different prices over time can only ever report the most recent one; there's no way to know what a specific unit actually cost when it sold. The real per-receipt cost already exists (`goods_received_line.cost_ex`, tied to a specific `goods_received` batch and `stock_id`), it's just never linked to an individual unit.

**The business's proposed fix** — print a special barcode per incoming unit encoding order number + SKU + serial, for 3 new printers being installed for stock processing — was evaluated and **not recommended**. It requires whoever applies the label to correctly match the right barcode to the right physical unit, which is exactly the failure mode the business itself flagged as likely. It also duplicates data that's already derivable from the database.

**Built instead, at the database layer rather than the label layer**:
- `serial_audit` gained `unit_cost` and `received_line_id` (FK to `goods_received_line`) — see `sql-scripts/create-serial-costing-extensions.sql`. `invoice_lines.serial_audit_id` (already present from the legacy port, 15,619 historical rows populated but never constrained) now has a real FK too, after nulling 1,018 orphaned values (6.5%, consistent with the ~5% orphan rate from the 2026-08-31 cross-database merge).
- A real gap was found and fixed while building this: **no code anywhere in the app created a `serial_audit` row** — receiving a serialized item was never wired up in this port at all. Goods Received now captures serial numbers per line (for `requiresserial` items) and stamps each new `serial_audit` row with that receipt's actual cost, with non-fatal warnings for a serial-count mismatch or a serial already recorded as in stock.
- At sale, the specific serial picked now supplies its own cost for `invoice_lines.cost_ex/cost_inc/sell_ex/sell_inc/gross_profit` — `stock.costprice` is no longer the source of truth for COGS/margin, only for defaulting the sell price on new sales. `serial_audit.is_in_stock`/`status` is now kept in sync on Sale (false/SOLD) and Refund (true/IN_STOCK) — previously never written at all, which meant the Sale tab's "available serials" picker (`SerialService.GetAvailableSerialsAsync`) was already silently relying on stale legacy data.
- Also fixed while in this area: `SerialService`'s "already sold" check queried `invoice_lines.serialnumber`, which is empty for all 14,578 historically-sold serials (they use the legacy `serial_number` column instead) — it now checks both, so a serial sold before this port existed is correctly detected as unavailable.
- New **Cost / Margin** report (`ReportsViewModel.RunCostMarginReport`) using the real per-unit figures, next to the existing reports.
- Ordinary product barcodes on the shelf didn't change — still scan-to-sell by SKU exactly as before. The only new physical step is printing a serial label at receiving time if the manufacturer doesn't already supply a scannable serial.

Verified end-to-end against live data: received a test serial at a distinct cost, sold it, refunded it, and hit the duplicate-serial warning path - confirmed correct cost/margin values at each step and that cleanup left no trace.

### 6.2 Central multi-store stock/pricing control (Fieldpine/Neto-style) — investigated 2026-09-01, plan below, not built

This matches the architecture already recommended back in Phase 0.1/Phase 4 — separate Postgres per store, plus a central reporting/control API layered on top later — so it doesn't need new architectural work to accommodate, just needs Phase 4 built out further than "reporting" into "control" (push pricing/stock changes out to stores, not just read them in).

**`franky-forge` (sibling project in IBG_HUB) is a much better fit than expected — verified by reading its actual code, not just its README**: it already has a multi-tenant product model (`tenants`, `products.tenant_id`), per-tenant *and* per-channel pricing markup rules, and a proven generalized channel-adapter pattern (`BaseChannel.syncProduct(product, existingExternalId, meta)`) with three shipped adapters (Shopify, BigCommerce, WooCommerce) plus idempotent per-product sync-status tracking (`product_channel_status`) and per-tenant encrypted credentials (`channel_configs`). On 2026-08-10 the project's own direction pivoted to exactly this shape: "tenant" was redefined from an internal segment to **"each buying-group member shop is a tenant"**, and there's live work (`franky-forge/CW_MASTER_DATA_ROADMAP.md`) solving the same shared-catalog-with-local-pricing-override problem for a different multi-franchisee buying group, using a `products.shared_group` tag mechanism on top of the tenant model.

**Instructive prior art**: a Fieldpine connector (referencing IBG_HUB's `everydaypos/` FD1 client) was scoped in franky-forge but **explicitly downgraded to speculative and deprioritized on 2026-08-12** — not because the idea was wrong, but because writing into Fieldpine's proprietary third-party API was an unverified, high-risk unknown with no proven write path. That risk doesn't apply here: IBG owns the JobMatix schema outright, so a JobMatix integration is actually the *lowest-risk* channel target attempted yet.

**Decision made (2026-09-01, by the business owner): a JobMatix store's stock items should be the *same* product records as the Neto/Shopify-facing catalog** — one shared product/pricing source of truth, not a separate catalog. Concrete plan, following that decision:

1. **Tenant mapping**: each IBG computer-repair store becomes one `franky-forge` tenant — reuses the existing tenant model as-is, no schema change needed there.
2. **Identity bridge**: add a nullable `stock.franky_product_id INTEGER` column in JobMatix, linking a locally-stocked item to its shared `franky-forge` `products.product_id` for centrally-managed SKUs. Nullable so repair-only parts (the long tail that will never appear in an online storefront) stay purely local and untouched.
3. **Auth**: reuse `franky-forge`'s existing `partner_api_keys` mechanism (already does per-tenant issuance/hashing) — issue one partner API key per JobMatix store.
4. **`franky-forge`'s read gap — ✅ built 2026-09-01, not yet pushed to origin**: `GET /partner/v1/products` added to `franky-forge/src/routes/partnerProducts.js` (that file was inbound-only before - a partner pushing their catalog *into* Franky for enrichment - and `products.js`'s GET routes are portal-staff-JWT-scoped, not partner-key-scoped, so this was real new work, not a config change). Supports `?updated_since=` for incremental sync and `?sku=a,b,c` for an exact batch lookup, applies any `product_overrides` price correction before markup, and computes price via the existing per-tenant/per-channel markup mechanism with a new `channel_type='jobmatix'` value (no schema change - reuses `channel_markup_rules` exactly like Shopify/BigCommerce/WooCommerce do). Deliberately ignores the `publish_enabled`/`channelPublishGate` check - that's about storefront readiness (image quality, confidence), irrelevant to whether a physical unit can be sold in-store. Tests added (`franky-forge/tests/partnerProducts.test.js`, router.stack-introspection pattern matching that repo's own convention); full suite (670 tests) still passes. **Committed locally in `franky-forge` but not pushed to origin/main** - that's a separate live production repo (shares a DB cluster with an unrelated live platform, and push may auto-deploy via DigitalOcean App Platform) - needs an explicit go-ahead before pushing, not routine JobMatix dev work.
   - Still needed on the `franky-forge` side before a JobMatix store can actually use this: issue a real `partner_api_keys` row for a store/tenant (existing admin UI, no code needed) and decide the `channel_markup_rules` value for `channel_type='jobmatix'` per tenant if a store's in-person price should differ from its Shopify one.
   - JobMatix side still needs: a small sync job (periodic or a manual "Sync Catalog" action) that calls this endpoint and updates `stock.sellprice`/`description` for linked items, plus the `stock.franky_product_id` link column from item 2 above. Not started.
5. **Pull, not push, for v1** — matches the hybrid local/cloud model the business also floated (local operation continues offline, catches up on reconnect). JobMatix pulling from Franky's API is inherently robust to a store being briefly offline; a live push *into* each store's database from outside is exactly the cross-network-write risk that got Fieldpine's connector deprioritized. `partner_api_keys.webhook_url`/`webhook_secret` (already built, currently unused for this) is a good fast-follow for near-real-time price pushes once the pull path is proven — not needed for v1.
6. **Sales/stock-movement reporting back centrally** (JobMatix → Franky) is a separate, later direction with no existing mechanism yet — deliberately out of scope for this phase.
7. **PriceIQ scan-back/promotion data (raised 2026-09-01, sequenced after/alongside this sync, not before it)**: the business intends to integrate a separate project, PriceIQ, with `franky-forge` to manage scan-back and promotion programs — supplier rebates tied to a product for a specific date range. Once that's attached to the shared product record, the same JobMatix↔Franky sync this phase builds needs to carry that data too, so a store's product sheet and (potentially) the POS scan-in flow show when an item is on promotion, for how long, and what the rebate is — not just the sell price. No schema or sync work has started on this; it depends on PriceIQ's own integration with `franky-forge` existing first, which hasn't been scoped yet either. Revisit once PriceIQ's data model in `franky-forge` is real, and extend the item-2/item-4 sync mechanism above (identity bridge + partner read endpoint) to include it rather than building a second parallel sync path.

**Status**: the `franky-forge`-side read endpoint is built and tested (pending push). Nothing on the JobMatix side is built yet - next concrete step when picked up: push the `franky-forge` commit (with go-ahead), issue a test partner API key, then build the `stock.franky_product_id` column + sync job here.

**Status**: not scoped into a session yet. Next step when picked up: (1) decide the `serial_audit` cost-tracking schema addition (own column vs. FK to `goods_received_line`), (2) build the Goods Received serial-capture step, (3) revisit Phase 4's central API scope once 6.1 is done and there's real per-store data worth centralizing.

---

## Risk Register (updated)

| Risk | Notes |
|---|---|
| Multi-store model decision (Phase 0.1) | Blocks Phase 4 design, does **not** block Phase 2/3. |
| Solo maintainer bus factor | One person + AI assistance. Keep things simple and documented. |
| Hardware procurement lead time | Order test units now, in parallel with software work. |
| ~~Cross-database access pattern~~ | Resolved by merging `jobmatix_jobs` into `jobmatix_pos` this session — JobMatix is one app over one database now, matching its original history. No cross-database design work left. |
| `GoodsInCare` schema decision (Phase 0.4) | Affects intake UI design directly — decide before building that screen, not after. |
| ~~Legacy report SQL (`SHAPE`/T-SQL scalar function)~~ | Resolved — the real parsing already lived in the legacy VB app, not SQL Server; ported to C# (`SessionTimesParser.cs`), verified at 99.95% against real data. |
| Two newly-found plaintext-credential gaps | `staff.password` (known) and `jobs.username`/`userpassword` (newly found, arguably worse — customer PC credentials). Fix both before real deployment. |
| RA attachment content not migrated | Metadata-only for all 150 historical attachment rows; check SQL Server backup recoverability before promising "view old attachments." |
| EWS/calendar retirement | Now moot — recommend dropping the integration outright rather than rebuilding against Graph. |
| ~~Live cutover / parallel-run~~ | Not applicable — greenfield. |
| ~~Printer/EFTPOS integration complexity~~ | Unchanged from v2 — lower risk than originally feared, well-understood CUPS/ESC-POS work. |
| ~~Zero FKs in Jobs database~~ | Resolved — was already fixed before this session, confirmed live. |
| ~~RA tightly coupled to Job Tracking~~ | Resolved by audit — RA is mostly independent (90.5% of real RAs have no job link), can ship on its own schedule. |
| ~~Serial-receiving pipeline doesn't exist yet (Phase 6.1)~~ | Resolved — Goods Received now creates `serial_audit` rows with real per-unit cost; Sale/Refund keep `is_in_stock`/`status` in sync. |
| Central catalog: reuse `franky-forge` (Phase 6.2) | Decided 2026-09-01 — reuse it, same product records as the Neto/Shopify catalog. The `franky-forge`-side read endpoint is built and tested but not pushed to origin yet (separate production repo, needs explicit go-ahead) - nothing on the JobMatix side is built. |

---

## Documentation Index

- `ROADMAP.md` — this file, the master plan
- `ROADMAP-ARCHIVE-2026-08.md` — superseded v2, kept for history
- `ROADMAP-ARCHIVE-2026-01.md` — superseded v1, kept for history
- `MIGRATION-STATUS.md`, `CURRENT-STATUS.txt` — **stale**, written against v1's assumptions, don't use for planning
- `JMxPOS8/CONVERSION_STATUS.md` — POS-specific progress detail
- `POSTGRESQL_MIGRATION_GUIDE.md` — still useful as SQL conversion reference
